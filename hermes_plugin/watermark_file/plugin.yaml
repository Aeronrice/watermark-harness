name: watermark-file
version: 1.0.0
description: "Agent tool for applying locked-standard repeated diagonal watermarks to local PDF and image files without overwriting originals."
author: local
kind: standalone
provides_tools:
  - watermark_file
pip_dependencies:
  - pillow>=10,<13
  - pypdf>=6.14.2,<7
  - reportlab>=5.0.0,<6
